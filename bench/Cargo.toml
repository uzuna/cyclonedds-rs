[package]
name = "cyclonedds-bench"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
cdr = "0.2.4"
cyclonedds-rs = { path = "../cyclonedds" }
cdds_derive.workspace = true
libc = "0.2"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0"
